#!/bin/bash
if [ -n "$TMUX" ]; then
  exec tmux choose-tree -Zws
fi

if ! tmux list-sessions >/dev/null 2>&1; then
  echo "tch: no tmux sessions" >&2
  exit 1
fi

exec tmux attach \; choose-tree -ZwsNN
